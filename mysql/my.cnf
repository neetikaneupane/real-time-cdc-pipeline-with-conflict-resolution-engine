[mysqld]
# Binary logging — required for Debezium
server-id           = 1
log_bin             = mysql-bin
binlog_format       = ROW
binlog_row_image    = FULL
expire_logs_days    = 10

# Required for Debezium to read table structure
binlog_rows_query_log_events = ON