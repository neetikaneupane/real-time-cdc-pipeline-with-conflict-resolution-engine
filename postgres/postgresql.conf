# Logical replication — these 3 lines are the critical ones
wal_level = logical
max_replication_slots = 4
max_wal_senders = 4

# Standard performance settings
max_connections = 100
shared_buffers = 128MB